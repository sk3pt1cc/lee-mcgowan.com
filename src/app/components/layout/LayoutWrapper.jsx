import styled from 'styled-components';

const LayoutWrapper = styled.div`
  height: 100%;
  overflow: scroll;
  a {
    color: white;
    border: none;
    outline: none;
  }
`;

export default LayoutWrapper;
