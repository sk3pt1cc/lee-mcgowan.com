import styled from 'styled-components';

const headerClr = '#0D1F2D';

const HeaderWrapper = styled.div`
  padding: 5px 10px;
  font-weight: bolder;
  font-size: 0.6rem;
  color: white;
  background-color: ${headerClr};
`

export default HeaderWrapper;