import styled from 'styled-components';
import { headerBarHeight } from '../header/HeaderWrapper';

const SocialsWrapper = styled.div`
  padding: 8px;
  background-color: #e8eaed;
  color: black;
  text-align: center;
  width: 100%;
  margin-top: ${headerBarHeight};
`;

export default SocialsWrapper;